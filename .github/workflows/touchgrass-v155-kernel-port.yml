name: Build TouchGrass safe diagnostic KSUN v3.2.0 legacy SUSFS v1.5.5

on:
  push:
    branches:
      - touchgrass-susfs-build
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-22.04
    timeout-minutes: 150

    steps:
      - name: Check out integration branch
        uses: actions/checkout@v4

      - name: Install build dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y bc bison build-essential cpio flex git libelf-dev libssl-dev patch python3 rsync unzip zip
          make --version | head -n1

      - name: Clone exact TouchGrass source
        run: |
          git init kernel
          cd kernel
          git remote add origin https://github.com/micr0softstore/samsung_android_kernel_a52xq.git
          git fetch --depth=1 origin 6bf351bdf18bdb228db79e66f14a7a9c0178e5d7
          git checkout --detach FETCH_HEAD
          git config user.name "TouchGrass Safe Diagnostic Builder"
          git config user.email "actions@users.noreply.github.com"

      - name: Apply exact SUSFS v1.5.5 Linux 4.19 implementation
        shell: bash
        run: |
          git init susfs-reference
          cd susfs-reference
          git remote add origin https://github.com/sakana164/kernel_xiaomi_sm8250.git
          git fetch --filter=blob:none --depth=2 origin af8fdfc7e9a3b6c67a8068f1d82c29db9e456022
          git checkout --detach FETCH_HEAD
          cd ..

          set +e
          git -C susfs-reference show --format= --binary af8fdfc7e9a3b6c67a8068f1d82c29db9e456022 \
            | patch -d kernel -p1 --batch --forward --fuzz=3 \
              > susfs-kernel-port.log 2>&1
          echo ${PIPESTATUS[1]} > susfs-kernel-port.status
          set -e

          find kernel -name '*.orig' -delete
          find kernel -name '*.rej' -delete

      - name: Import KernelSU-Next v3.2.0-legacy
        run: |
          git init ksun-reference
          cd ksun-reference
          git remote add origin https://github.com/crdroidandroid/android_kernel_oneplus_sm8350.git
          git config core.sparseCheckout true
          printf 'drivers/kernelsu/\n' > .git/info/sparse-checkout
          git fetch --depth=1 origin b0e9f56d42a064443cdf4ace2187595eb04544e9
          git checkout --detach FETCH_HEAD
          cd ..

          rm -rf kernel/KernelSU kernel/drivers/kernelsu
          mkdir -p kernel/KernelSU/kernel
          cp -a ksun-reference/drivers/kernelsu/. kernel/KernelSU/kernel/
          ln -s ../KernelSU/kernel kernel/drivers/kernelsu

          grep -q 'obj-$(CONFIG_KSU).*kernelsu' kernel/drivers/Makefile || \
            printf '\nobj-$(CONFIG_KSU) += kernelsu/\n' >> kernel/drivers/Makefile
          grep -q 'drivers/kernelsu/Kconfig' kernel/drivers/Kconfig || \
            printf '\nsource "drivers/kernelsu/Kconfig"\n' >> kernel/drivers/Kconfig

      - name: Run compatibility integration and apply safe diagnostic profile
        shell: bash
        run: |
          base64 -d touchgrass-port/integrate_exact.py.gz.b64 | gzip -d > integrate_exact.py
          python3 -m py_compile integrate_exact.py
          KERNEL_ROOT=kernel python3 integrate_exact.py 2>&1 | tee exact-integration.log
          base64 -d touchgrass-port/fix_fdinfo_v2.py.b64 | python3
          python3 touchgrass-port/apply_safe_diagnostic.py | tee safe-diagnostic.log

      - name: Audit versions and reduced feature configuration
        run: |
          grep -q '#define SUSFS_VERSION "v1.5.5"' kernel/include/linux/susfs.h
          grep -q 'KSU_GIT_TAG := v3.2.0-legacy' kernel/KernelSU/kernel/Kbuild
          grep -q '^CONFIG_KSU=y' kernel/arch/arm64/configs/a52xq_defconfig
          grep -q '^CONFIG_KSU_MANUAL_HOOK=y' kernel/arch/arm64/configs/a52xq_defconfig
          grep -q '^CONFIG_KSU_SUSFS=y' kernel/arch/arm64/configs/a52xq_defconfig
          grep -q '^# CONFIG_KSU_SUSFS_SUS_SU is not set' kernel/arch/arm64/configs/a52xq_defconfig
          grep -q '^# CONFIG_KSU_SUSFS_SUS_PATH is not set' kernel/arch/arm64/configs/a52xq_defconfig
          grep -q '^# CONFIG_KSU_SUSFS_SUS_KSTAT is not set' kernel/arch/arm64/configs/a52xq_defconfig
          grep -q '^# CONFIG_KSU_SUSFS_TRY_UMOUNT is not set' kernel/arch/arm64/configs/a52xq_defconfig
          grep -q '^# CONFIG_KSU_SUSFS_AUTO_ADD_TRY_UMOUNT_FOR_BIND_MOUNT is not set' kernel/arch/arm64/configs/a52xq_defconfig
          grep -q '^# CONFIG_KSU_SUSFS_AUTO_ADD_SUS_BIND_MOUNT is not set' kernel/arch/arm64/configs/a52xq_defconfig
          grep -q '^# CONFIG_KSU_SUSFS_OPEN_REDIRECT is not set' kernel/arch/arm64/configs/a52xq_defconfig
          cmp -s ksun-reference/drivers/kernelsu/feature/kernel_umount.c kernel/KernelSU/kernel/feature/kernel_umount.c
          cmp -s ksun-reference/drivers/kernelsu/hook/setuid_hook.c kernel/KernelSU/kernel/hook/setuid_hook.c

          {
            echo 'Kernel base: 6bf351bdf18bdb228db79e66f14a7a9c0178e5d7'
            echo 'KernelSU-Next: v3.2.0-legacy'
            echo 'SUSFS: v1.5.5'
            echo 'Hook mode: manual'
            echo 'Profile: reduced-feature diagnostic'
            echo 'Original KernelSU setuid/unmount path: restored'
            echo 'Disabled: SUS_PATH, SUS_KSTAT, TRY_UMOUNT, AUTO_ADD_TRY_UMOUNT, AUTO_ADD_BIND_MOUNT, OPEN_REDIRECT, SUS_SU'
            echo
            grep -RIn 'ksu_handle_' kernel/fs/exec.c kernel/fs/open.c kernel/fs/read_write.c kernel/fs/stat.c kernel/drivers/input/input.c kernel/kernel/reboot.c || true
          } > safe-integration-audit.txt

      - name: Build kernel using original source script
        shell: bash
        run: |
          set +e
          cd kernel
          rm -rf out
          chmod +x build_kernel.sh tools/dtc 2>/dev/null || true
          bash build_kernel.sh 2>&1 | tee ../safe-build.log
          status=${PIPESTATUS[0]}
          echo "$status" > ../safe-build.status
          exit 0

      - name: Prepare diagnostics and patch
        if: always()
        shell: bash
        run: |
          cd kernel
          git add -N . 2>/dev/null || true
          git diff --binary -- . ':!toolchain' ':!out' > ../touchgrass-safe-diagnostic.patch
          cd ..
          tail -n 600 safe-build.log > safe-build-tail.log 2>/dev/null || true
          {
            echo "Build status: $(cat safe-build.status 2>/dev/null || echo missing)"
            echo "Image present: $(test -f kernel/out/arch/arm64/boot/Image && echo yes || echo no)"
            echo "Image size: $(stat -c %s kernel/out/arch/arm64/boot/Image 2>/dev/null || echo 0)"
            echo "SUSFS version: $(sed -n 's/^#define SUSFS_VERSION "\([^"]*\)"/\1/p' kernel/include/linux/susfs.h | head -n1)"
            echo "KernelSU tag: $(sed -n 's/^KSU_GIT_TAG := //p' kernel/KernelSU/kernel/Kbuild | head -n1)"
            echo 'Profile: reduced-feature diagnostic'
          } > safe-build-report.txt

      - name: Upload safe diagnostic kernel result
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: touchgrass-safe-diagnostic-${{ github.run_number }}
          retention-days: 14
          if-no-files-found: warn
          compression-level: 9
          path: |
            exact-integration.log
            safe-diagnostic.log
            safe-integration-audit.txt
            safe-build.status
            safe-build-report.txt
            safe-build.log
            safe-build-tail.log
            susfs-kernel-port.log
            susfs-kernel-port.status
            touchgrass-safe-diagnostic.patch
            kernel/out/.config
            kernel/out/arch/arm64/boot/Image
